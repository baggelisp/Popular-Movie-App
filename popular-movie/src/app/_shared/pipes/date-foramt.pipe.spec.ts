import { DateForamtPipe } from './date-foramt.pipe';

describe('DateForamtPipe', () => {
  it('create an instance', () => {
    const pipe = new DateForamtPipe();
    expect(pipe).toBeTruthy();
  });
});
